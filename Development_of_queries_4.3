//Request to add a new student
INSERT INTO Students (ID, first_name, last_name, email, password, class) 
VALUES ('6', 'John', 'Doe', 'john.doe@gmail.com', 'password123', '10A');

//Request to add a new item
INSERT INTO Subjects (ID, name, description) 
VALUES ('4', 'Chemistry', 'The study of matter and its properties');

//Request to add a new teacher
INSERT INTO Teachers (ID, first_name, last_name, email, password) 
VALUES ('4', 'Jane', 'Smith', 'jane.smith@gmail.com', 'password123');

//Request to add a grade for a specific student and subject
INSERT INTO Grades (ID, student_id, subject_id, grade, date) 
VALUES ('6', '1', '3', '95', '2023-04-24');

//Request to receive the student's average grade for a certain period for a specific subject
SELECT AVG(grade) 
FROM Grades 
WHERE student_id = '1' AND subject_id = '3' AND date >= '2023-04-01' AND date <= '2023-04-30';

//Request to receive a list of students with their average grades for each subject
SELECT Students.first_name, Students.last_name, Subjects.name, AVG(Grades.grade) 
FROM Students 
JOIN Grades ON Students.ID = Grades.student_id 
JOIN Subjects ON Subjects.ID = Grades.subject_id 
GROUP BY Students.ID, Subjects.ID;

//Request to receive a schedule for a specific day of the week
SELECT Schedule.ID, Subjects.name, Teachers.first_name, Teachers.last_name, Schedule.day_of_week, Schedule.start_time, Schedule.end_time 
FROM Schedule 
JOIN Subjects ON Subjects.ID = Schedule.subject_id 
JOIN Teachers ON Teachers.ID = Schedule.teacher_id 
WHERE Schedule.day_of_week = 'Monday';
